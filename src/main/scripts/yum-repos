#!/usr/bin/env python
from yumrepos import app
from yumrepos import FsBackend, app_configure

app_configure(FsBackend('/tmp/repos', 'createrepo_c'), serve_static=True)

app.run(
    host="0.0.0.0",
    port=int("8080"),
    debug=True
)
